---
globs: tools/make_*.py, tools/*.py, assets_src/**
---
**the asset, audio and image pipelines** → read `spec/systems/assets-and-audio.md` before changing anything here (SPEC-FIRST: plan the change in the spec first, record it after).
